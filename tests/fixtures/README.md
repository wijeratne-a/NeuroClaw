# Test fixtures

Optional: place a short (e.g. 5s) CC0 test clip here as `sample.mp4` and record its SHA-256 in integration tests.

Local `scripts/verify_shapes.py` and full TRIBE runs require `HF_TOKEN` and sufficient RAM; CI uses mocks only.
